server {
    listen       80;
    server_name  localhost;

    location /drizzleangular {
        root   /usr/share/nginx/html;  # Adjust to the location of your app
        index  index.html;
    }

    location /drizzleangular/healthz {
        root   /usr/share/nginx/html;  # Adjust to serve health check if needed
        index  healthz.html;  # Custom health check page if required
    }

    # Further customizations can go here
}

